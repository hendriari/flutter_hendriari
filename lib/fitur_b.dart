import 'package:flutter/material.dart';

class FiturB extends StatelessWidget {
  const FiturB({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ini halaman Fitur B'),
      ),
      body: const Center(
        child: Text('ini halaman fitur B'),
      ),
    );
  }
}
